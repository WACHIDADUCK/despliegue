name: deploy
on:
  workflow_dispatch:

  jobs:
    deploy:
    name: Deploy to VPS
    runs-on: ubuntu-22.04
    with:
      host: 161.22.42.207
      port: 2222
      username: wachi
      key: ${{secrets.PRIVATE_KEY}}
      script: "cd /var/www/html/despliegue; git pull origin main; npm run build"